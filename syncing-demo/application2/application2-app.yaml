apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: application2-app
  namespace: openshift-gitops
spec:
  destination:
    name: wl00
    namespace: application2-httpd
  project: sync-demo
  source:
    path: syncing-demo/application2/base
    repoURL: 'https://github.com/robertsandoval/gitops.git'
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

